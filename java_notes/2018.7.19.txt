关于String用byte数组构造串的构造方法的问题：
该方法是将byte数组中的字节按照Unicode编码方式编码字符串 那么问题来了   //InputByString.java
计算机怎么知道哪两个字节应该组成一个双字节对应的字符 又怎么知道哪两个字节本来就是分开的每个字节分别对应一个字符？
★通过控制台输出测试和代码测试发现好像byte字节数组中连续两个值为负的元素值可以编码成一个汉字字符  //StringTest.java
是不是和计算机中的补码什么的有关？

还有就是关于InputByString这个代码的诸多问题（其实就是逻辑还没弄得特别清晰）
比如对于count的值的判定是不是只有每次按下回车时候才会判定？

IO口这块书上关于文件类的介绍和相关部分内容我先不看了吧...进度有点太慢了...今天下午直接开新章节...网络通信
据说主要其实是偏网络这块的...

中午眯了会清醒过后突然想到了以前C语言里面的scanf和printf
然后java标准输入输出流又说是预输入缓输出的 也就是由缓存区
那么我能不能这样理解，我在控制台输入数据，System.in.read（）先把输入的数据放入某个对应的缓存区，只有在按下回车的时候才会执行接下来的语句？
联想一下以前C语言的scanf语句，好像是这样的，那么如果是这样的话对于无论是count啊i啊这些变量的判断，好像一切就都说得通了哎，只有在输入的过程
中按下回车键才会去判断哎。

下午时间又tm莫名其妙被占用了，你说你tm要不然让我能学到东西再叫我，不然你就让我自己看书不行吗，艹。
看了一点点URL类还有URLConnection类的内容，URL就相当于构造文件时候用的文件名的字符串，URLConnection就相当于是文件了（好别扭啊...）
和文件类比的过程中不同的是：
文件类实例即为文件，在文件类的构造方法中可以直接用文件名（字符串）构造，这样类比的话按理说URLConncetion的实例也可以用URLConnection类的
构造方法直接用URL实例构造，但是并不可以，实际上URLConnection类的实例是由URL类中的openConnection（）方法返回一个URLConnection类实例
一个URL实例对象仅表示一个网络资源，获取资源内容的操作还是需要使用流，URL类有openStream（）方法返回一个InputStream实例

关于主机名、ip、域名、url的关系
是不是一个网络上的主机对应一个ip，对应一个域名，这个主机（计算机）上有许多文件，每个文件对应一个url？域名和主机名又是什么区别啊我知道域名
可以通过dns解析成ip什么的。
主机是不是就是服务器啊？