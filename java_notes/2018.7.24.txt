是不是服务端创建ServerSocket时候所用的端口号就是该对象调用accetp（）方法时创建的Socket对象的端口号啊？
直接用Socket方法构造出来的Socket对象，其构造方法中的端口号并不是自己的端口号（是吗？），而是指定服务器主机的端口号，也就是ServerSocket的
端口号？(因为你创建Socket实例就是为了通信，也就是在创建Socket的同时就是为了请求与对方建立连接，所以用于构造Socket实例的端口号是指定对方的
端口号而不是自己的?自己的端口号是随机生成的？)→自己这边的端口号好像确实是随机的...
是不是一个主机有好多好多端口，一个Socket只对应其中一个？

总感觉又快要放弃了哎
每一次都是这样一开始干劲十足热情满满，慢慢地就激情被磨去，一点点提不起兴趣，可是我也不想装作自己还很有兴趣的样子...
可能注定自己就是这样的人？或者说只是其实早就已经被同化了只能说说这样的话来安慰一下自己，给自己找找借口？我也不知道。